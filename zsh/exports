export GITHUB_USER="avidal"
export TERM="screen-256color"
export DISPLAY=:0.0
export EDITOR=vim

# apparently setting EDITOR to vim causes zsh to switch to vim keybindings
# use bindkey -e to reset it back to normal
bindkey -e

home_path=$HOME/.local/bin:$HOME/bin
local_path=/usr/local/bin:/usr/local/sbin
export PATH=$home_path:$local_path:$PATH:/usr/lib/git-core

export GOPATH=$HOME/Code

export PATH=$GOPATH/bin:/usr/local/opt/coreutils/libexec/gnubin:$PATH

source ~/.nvm/nvm.sh

# dircolors isn't on the $PATH on the mbp until we add the brew gnubin path
# above
eval `dircolors ~/.dircolors`

export DOCKER_HOST=tcp://192.168.59.105:2376
export DOCKER_CERT_PATH=/Users/alex/.boot2docker/certs/boot2docker-vm
export DOCKER_TLS_VERIFY=1
